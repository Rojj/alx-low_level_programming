#include <stdio.h>

/**
 * main - prints the phrase with propergrammar ,
 * but the outcome is a piece of art
 * Return: 0 if exited, otherwise non-zero
 */
int main(void)
{
	printf("with prper grammar , but the 
			outcome is a piece of art,\n")
		return	(0)
}
